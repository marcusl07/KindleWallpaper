When a task is completed, run `scripts/archive_task.sh <TASK_ID>` using the full task ID (for example `T99-a`) to move it out of `tasks-active.txt` and append it to `tasks-archive.txt`.
Tasks are grouped into parallel waves by numeric stem. Tasks that share the same numeric stem (for example `T99-a` and `T99-b`) are intended to be independently executable in parallel.
Only put tasks in the same numeric wave if their primary file ownership is disjoint. If two tasks would mainly edit the same file, they must be merged or moved to different numeric waves.
Prefer completing the full current wave before starting the next numeric stem unless a section explicitly says otherwise.
Do not leave completed tasks in `tasks-active.txt`.

## Auto-launch On Startup

**T94-a — Add launch-at-login AppState and ServiceManagement plumbing**
Implement launch-at-login service for KindleWall using `ServiceManagement.SMAppService.mainApp`, with AppState plumbing for current enabled state, refresh, toggle, and user-visible error handling.

**T94-b — Add Settings UI for launch-at-login**
Add a Settings toggle in the About section for launching KindleWall at login, including helper copy about macOS Login Items and inline error/status messaging from AppState.

**T95-a — Verify launch-at-login behavior end to end**
Verify KindleWall launch-at-login behavior on macOS, including enable, disable, restart/login behavior, accessory-app launch behavior, and compatibility with the existing On app launch wallpaper rotation mode.

## Filter Row Overflow

**T96-a — Spec out filter row overflow**
Spec out the horizontal overflow issue in the filter row where the controls require side-scrolling with no indication that more content is offscreen.

## Bulk Delete

**T99-a — Implement transactional bulk book delete with exact affected-quote capture**
Add database and app-state APIs for deleting multiple selected books by first capturing the exact currently live linked quote IDs, then in one transaction inserting tombstones for that captured set, deleting those quote rows, and deleting the selected book rows. The operation should use the captured set so the affected-quote count shown in confirmation matches the rows actually deleted.

**T100-a — Add Quotes edit mode and explicit bulk-delete selection flow**
Add multi-select edit mode to the Quotes screen with a visible but disabled bulk-delete toolbar action when selection is empty. Bulk delete must operate only on explicitly selected rows, not the filtered result set, and selection should preserve still-valid IDs across list refreshes while dropping rows that no longer exist.

**T101-a — Add Books edit mode and explicit bulk-delete selection flow**
Add multi-select edit mode to the Books screen with the same explicit-selection behavior as Quotes and a visible but disabled bulk-delete toolbar action when selection is empty. Preserve valid selections across refreshes, drop selections for removed rows, and leave newly imported rows unselected while edit mode is active.

**T102-a — Add count-based confirmation for quote bulk delete**
For Quotes, show a two-step destructive confirmation using the exact captured selected quote count.

**T103-a — Add count-based confirmation for book bulk delete**
For Books, show a two-step destructive confirmation using the exact captured selected book count plus the exact number of currently live linked quotes that will be deleted; do not count previously deleted or tombstoned quotes.

**T104-a — Verify bulk delete, suppression, and selection-state behavior**
Add or update verification coverage for tombstone matching across re-import, transactional rollback safety for bulk book delete failures, exact confirmation counts, disabled empty-selection toolbar state, and edit-mode selection reconciliation when rows are deleted or newly imported during list refresh.

## DB Performance

**T105-a — Spec out DB performance improvements**
Spec out DB performance improvements.
